spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: ranchel
    url: jdbc:mysql://localhost:3306/food_project?characterEncoding=utf-8&useSSL=false
  jpa:
    show-sql: true

